interface:
  display_name: "Envctl"
  short_description: "Repo-specific patterns and workflows for envctl"
  default_prompt: "Use the envctl repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true